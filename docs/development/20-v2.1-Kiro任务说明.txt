Continue the authorized MakerMuse hackathon project at /Users/pengranjun/Documents/Codex/2026-09-18/w-x20/outputs/memory-theatre. You own ONLY src/Scene.jsx and src/StandPreview.jsx, optionally one focused helper src/render-loop.js plus a meaningful node test tests/render-loop.test.mjs if needed. You are not alone; Codex is changing main.jsx, Production.jsx and CSS. Do not revert others or edit their files. Prior UPGRADE.md ownership was for an earlier task; this assignment supersedes its scope for this turn.

Improve these existing previews to stop GPU rendering when idle, offscreen or document hidden. Preserve current attractive geometry, shadows, lighting, orbit controls with damping, explode transitions, active-layer highlights, reset and responsiveness. Scene currently draws on every RAF; StandPreview too. Use a small reliable invalidation mechanism: interactions/resize/prop changes wake rendering; continue only while interpolation or controls damping is moving, then settle and stop. IntersectionObserver + visibilitychange pause hidden views and wake when visible. Handle reduced-motion pref if straightforward without changing default style. Dispose listeners, RAF, observers and resources correctly. Stand WebGL creation failure should display a clear graceful fallback instead of an empty panel. Do not use polling intervals. Do not introduce a framework or external dependency. Do not use CLI shell tools (not available). Make only assigned edits and finish with precise changes and limitations. Codex will run browser instrumentation, interactions and tests. Runtime hooks for tests are unnecessary.
